political_actions = {

	XSK_ai_integrate_mongolia = {

		icon = GFX_decision_generic_nationalism

		priority = 50

		allowed = {
			tag = XSK
		}

		available = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
			any_country = {
				is_subject_of = ROOT
				original_tag = XON
			}
		}

		visible = { 
			OR = {
				is_ai = yes
				is_debug = yes
			}
			any_country = {
				is_subject_of = ROOT
				original_tag = XON
			}
		}

		fire_only_once = no

		cost = 50

		days_re_enable = 30

        ai_will_do = {
			base = 10000
		}
		complete_effect = {
			every_country = {
				limit = {
					is_subject_of = ROOT
					original_tag = XON
				}
				add_autonomy_score = {
    				value = -500
				}
			}
			every_country = {
				limit = {
					is_subject_of = ROOT
					original_tag = XON
					compare_autonomy_progress_ratio > 0.9
				}
				ROOT = {
					annex_country = {
    					target = PREV
    					transfer_troops = yes
					}
				}
				every_core_state = {
					add_core_of = ROOT
				}
			}
        }

	}

	XSK_vilayet_of_chahar  = { 
		
		icon = GFX_decision_generic_nationalism

		priority = 50

		allowed = {
			tag = XSK
		}

		highlight_states = { 
			highlight_state_targets = {
				state = 611 
				state = 612
				state = 621
			}
		}

		available = {
			611 = { is_owned_and_controlled_by = ROOT }
			612 = { is_owned_and_controlled_by = ROOT }
			621 = { is_owned_and_controlled_by = ROOT }
		}

		visible = { 
			is_subject = no
		}

		fire_only_once = yes

		cost = 0

        ai_will_do = {
			base = 100
		}

		complete_effect = {
			add_war_support = 0.05
			add_stability = 0.05
			custom_effect_tooltip = XSK_vilayet_of_chahar_tt
			hidden_effect = {
				create_dynamic_country = {
    				original_tag = XMJ
					set_cosmetic_tag = XSK_chahar
					every_core_state = {
						remove_core_of = PREV
					}
    				transfer_state = 611
					611 = {
						add_core_of = THIS
					}
					transfer_state = 612
					612 = {
						add_core_of = THIS
					}
					transfer_state = 621
					621 = {
						add_core_of = THIS
					}
					ROOT = {
						set_autonomy = {
    						target = PREV
    						autonomous_state = autonomy_XSK_vilayet
    						end_wars = no
    						end_civil_wars = no
						}
					}
					set_politics = {
						ruling_party = neutrality
						elections_allowed = no
						long_name = "Ottoman Rule"
						name = "Ottoman Rule"
					}
					create_country_leader = {
						name = "Saidullah Dawamet"
						picture = GFX_portrait_XSK_ottoman_vassal_3
						expire = "3000.1.1"
						ideology = despotism
						traits = {
							XSK_ottoman_subject_trait
						}
					}
				}
			}
        }
	}

	XSK_integrate_vilayets  = { 
		
		icon = GFX_decision_generic_form_nation

		priority = 50

		allowed = {
			tag = XSK
		}

		highlight_states = { 
			highlight_state_targets = {
				state = 611 
				state = 612
				state = 621
			}
		}

		target_trigger = {
			FROM = {
				has_autonomy_state = autonomy_XSK_vilayet
				OR = {
					has_cosmetic_tag = XSK_chahar
				}
			}
		}

		available = {
			FROM = {
				compare_autonomy_progress_ratio < 0.1
				has_capitulated = no
			}
		}

		visible = { 
			
		}

		fire_only_once = yes

		cost = 100

        ai_will_do = {
			base = 100
		}

		complete_effect = {
			annex_country = {
    			target = FROM
    			transfer_troops = yes
			}
			FROM = {
				every_core_state = {
					add_core_of = ROOT
					remove_core_of = FROM
				}
			}
        }
	}

}